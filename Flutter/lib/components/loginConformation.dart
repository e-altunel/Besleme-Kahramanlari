/*import 'package:htpp/htpp.dart' as http;

class loginConformation {
  _logIn(String mail, String password) async {
    htpp.Response sonuc = await htpp.post(Uri.parse(api_link ),body: {
      "user_mail": mail,
      "user_password": password,
    });
    if (sonuc.statusCode == 200) {
      print(sonuc.body);
    } else {
      return [false, "We cannot process your transaction right now."];
    }

}*/
